# Архив тестов прототипа ADR-009

`adr009-prototype.ts.disabled` сохранён для истории. Он использует удалённые
Map-хранилище, AI-port и x-demo-role/x-demo-actor headers, поэтому не запускается
как тест рабочего PostgreSQL BFF. Исходные 22 успешные проверки относятся только
к тому прототипу. Действующие domain-тесты остаются в src/, HTTP-сценарий —
в scripts/integration-http.ts; после объединения с PR #15 проверки не запускались
по просьбе пользователя. Архив нельзя подключать как production runtime.
